 <h1>Users List</h1>

   <% @users.each do |user| %>
     <p><%= link_to user.email, user, style: "z-index:" %></p>
     <% if current_user.following?(user) %>
    <%= button_to "Unfollow", unfollow_path(user) %>

<% else %>
    <%= button_to "follow", follow_path(user) %>
<% end %>
   <% end %>