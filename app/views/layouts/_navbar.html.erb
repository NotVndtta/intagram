<%= stylesheet_link_tag("navbar.css")%>
<nav class="navbar navbar-expand-lg navbar-light bg-light">
<% if user_signed_in?%>
  <%= link_to "Home", root_path, class: "navbar-brand"%>
  <%= link_to "Users List", search_users_path, class: "navbar-nav"%>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
     <%= form_with url: search_users_path, method: :get do |form| %>
    <%= form.search_field :query, value: params[:query], placeholder: "User List", class:"search-field" %>
    <%= form.submit "User List", class:"btn btn-primary" %>
  <% end %>
   <%= link_to "My Posts", posts_path, class: "navbar-nav"%>
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNav">
    <ul class="navbar-nav">
      <li class="nav-item active">
      </li>
    </ul>
    <% end %>
  </div>
</nav>